{{assign pageTitle='requests' currentPageUrl="/requests.html"}}

{{! define the block with page-specific styles }}
{{#partial 'styles'}}
<link href="./requests/requests.scss" rel="stylesheet">
{{/partial}}

{{! define the block with page-specific scripts }}
{{#partial 'scripts'}}
<script src="./requests/requests.js" defer></script>
{{/partial}}

{{! define the block content: include the `requests/content.hbs` partial }}
{{#partial 'content'}}
{{> requests/content}}
{{/partial}}

{{! extends the layout template with blocks defined above }}
{{> layout}}
