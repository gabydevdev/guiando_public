{{assign pageTitle='welcome!' currentPageUrl="/"}}

{{! define the block content: include the `content.hbs` partial }}
{{#partial 'content'}}
{{> bookings-table}}
{{/partial}}

{{! extends the layout template with blocks defined above }}
{{> layout}}
